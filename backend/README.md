# Backend

FastAPI service for project, song, analysis, generation and rendering orchestration.

The backend will own the API contract and job lifecycle. AI providers and rendering engines will be accessed through explicit service interfaces rather than directly from route handlers.
